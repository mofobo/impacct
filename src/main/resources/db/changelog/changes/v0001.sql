create table exam (
                      id BIGINT not null,
                      title varchar(50) not null,
                      description varchar(512) not null,
                      primary key (id)
);